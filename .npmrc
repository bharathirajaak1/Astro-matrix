; react-dom (pulled transitively by expo-router's web deps) requests a newer
; react patch than react-native pins. The mismatch is benign for a native
; Expo Go app, so peer resolution is relaxed to keep installs working.
legacy-peer-deps=true
